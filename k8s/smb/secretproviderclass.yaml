apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: notiflex-secrets
  namespace: notiflex
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "projects/claude-study-501117/secrets/valkey-password/versions/latest"
        path: "valkey-password"
